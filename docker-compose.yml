services:
  ollama:
    image: ollama/ollama:latest
    container_name: secret-ollama
    profiles:
      - llm
    restart: unless-stopped
    environment:
      OLLAMA_HOST: 0.0.0.0
    ports:
      - "11434:11434"
    volumes:
      - ollama_data:/root/.ollama

volumes:
  ollama_data:
